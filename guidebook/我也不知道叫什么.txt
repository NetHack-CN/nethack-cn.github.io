
.lp #annotate
命名当前层。所有地牢层的注释将会通过‘#overview’命令进行展示
自动完成。认的按键是‘M-A’，如果
.op number_pad
选项启用的时候，也可以使用按键‘^N’。
.lp "#apply   "
激活（使用）工具（鹤嘴锄,钥匙,提灯...）。
默认按键为‘a’。
.lp ""
如果这个工具作用于在地上的物品，使用‘m’前缀来跳过这个物品。
.lp ""
如果选择一把魔杖，该魔杖将会被破坏，并在这个过程中施放它的魔法。操作前会要求确认。
.lp #attributes
显示你的属性。
默认按键为‘^X’。
.lp #autopickup

.op autopickup
或关闭。
默认按键为‘@’。
.lp "#call    "
命名一个怪物，或者一件在物品清单的，或者在地上的，或者在发现列表中的物品，或者为当前的地牢层添加一个注释（和“#annotate”一样）。
默认按键为‘C’。
.lp "#cast    "
施放魔法。
默认按键为‘Z’。
.lp "#chat    "
谈话。
默认按键为‘M-c’。
.lp "#close   "
关门。
默认按键为‘c’。
.lp "#conduct "
列举你坚持下来的自愿挑战。
自动完成。
默认按键为‘M-C’。
.lp ""
查看下文标题为“Conduct”的部分获取详细信息。
.lp "#dip     "
将某物浸入某物中。
默认按键为‘M-d’。
.lp "#down    "
下楼梯。
默认按键为‘>’。
.lp "#drop    "
丢下物品。
默认按键为‘d’。
.lp #droptype
丢下特定种类的物品。
默认按键为‘D’。
.lp "#eat     "
吃东西。
默认按键为‘e’。添加‘m’前缀将会跳过地板上的物品。
.lp "#engrave "
在地板上刻字。
默认按键为‘E’。
.lp "#enhance "
提升或检查武器和魔法技能。
自动完成。
默认按键为‘M-e’。
.lp #exploremode
进入探索模式。
.lp ""
会进行确认；默认的回应是\f(CRn\fP（no）。
想要真正的切换到探索模式，需要回答y。你可以通过设置
.op paranoid_confirmation:quit
选项来要求输入\f(CRyes\fP而不是\f(CRy\fP来回答。
.lp "#fire    "
从箭袋中发射发射物。
默认按键为‘f’。
.lp "#force   "
暴力开锁。
自动完成。
默认按键为‘M-f’。
.lp "#glance  "
显示地图符号对应的事物。
默认按键为‘;’。
.lp "#help    "
获取帮助信息。
默认按键为
默认按键为‘?’，如果
.op number_pad
选项启用的情况下也可以使用‘h’。
.lp #herecmdmenu
显示此处可执行的指令菜单。
.lp "#history "
显示游戏开发历史的概述。
默认按键为‘V’。
.lp #inventory
显示你的物品栏。
默认按键为‘i’。
.lp #inventtype
显示物品栏中某特定类别的物品。
默认按键为‘I’。
.lp "#invoke  "
激活物品的特殊能力。
自动完成。
默认按键为‘M-i’。
.lp "#jump    "
跳到另一处。
自动完成。
默认按键为‘M-j’，如果
.op number_pad
启用也可以使用‘j’。
.lp "#kick    "
用脚踢。
默认按键为‘^D’，如果
.op number_pad
选项启用也可以使用‘k’。
.lp "#known   "
显示已发现的物品类型。
默认按键为‘\’。
.lp #knownclass
显示某类物品的已发现类型。
默认按键为‘`’。
.lp #levelchange
修改经验等级。
自动完成。调试模式专属。
.lp #lightsources
显示移动光源。
自动完成。调试模式专属。
.lp "#look    "
查看这里的物品。
默认按键为‘:’。
.lp "#loot    "
搜刮地上的箱子。
自动完成。加上’m‘前缀来跳过在你位置上的容器并直接移除马鞍。
默认按键为‘M-l’，如果
.op number_pad
选项启用也可以使用‘l’。
.lp "#monster "
使用怪物的特殊能力。
自动完成。
默认按键为‘M-m’。
.lp "#name    "
同\#call;命名一个怪物,特定物品,或者一类物品。和‘#call’一样。
自动完成。
默认按键为‘N’、‘M-n’和‘M-N’。
.lp "#offer   "
向神提供一个祭品。
自动完成。
默认按键为‘M-o’。
.lp ""
你需要找到一个祭坛才能够有机会进入下一步。你最近杀死的怪物的尸体是这个祭坛的敲门砖。
.lp ""
‘m’作为命令的前缀将会跳过提供任何在祭坛上的物品。
.lp "#open    "
开门。
默认按键为‘o’。
.lp "#options "
显示和修改选项设置。
默认按键为‘O’。
.lp #overview
显示你发现的关于地牢的信息。任何到过的有注释的层（除非因为遗忘而忘记）也会被包括在内，以及许多的事物（祭坛，王座，喷泉等等；额外的通向另一个地牢分支的楼梯）。
如果地牢概观在游戏结束展示期间被选择，一切到过的层不管是否有注释都将被包括在内进行显示。
自动完成。
默认按键为‘^O’，and ‘M-O’。
." DON'T PANIC!
.lp "#panic   "
测试常规的恐慌按键。终止当前的游戏。
自动完成。调试模式专属。
.lp ""
会被要求进行确认；默认是n（no）；继续游戏。为了真正的恐慌，需要回答y。你可以设置选项
.op paranoid_confirmation:quit
来要求用\f(CRyes\fP替代y进行回答。
.lp "#pay     "
付款。
默认按键为‘p’。
.lp "#pickup  "
捡起在当前位置的物品。
默认按键为‘,’。加上‘m’前缀将强制使用一个菜单。
.lp #polyself
变形自己。
自动完成。调试模式专属。
.lp "#pray    "
向神祈祷以获取帮助，
自动完成。
默认按键为‘M-p’。
.lp ""
在先前获得帮助后立马再次进行祈祷不是一个好主意。
（注意：活着进入地牢被认为已经获得了帮助。你不应该在开启新一轮游戏后立马进行祈祷。）
偶然的使用这个命令会造成麻烦，因此这儿有个选项可以让你确认你想要进行祈祷的意图。
该选项默认开启，你可以重置
.op paranoid_confirmation
选项来禁用它。
.lp "#prevmsg "
查看最近的游戏消息。
默认按键为‘^P’。
.lp "#puton   "
戴上一件配饰（戒指,护身符等）。
默认按键为‘P’。
.lp "#quaff   "
喝东西。
默认按键为‘q’。
.lp "#quit    "
不保存直接退出。
自动完成。
默认按键为‘M-q’。
.lp ""
一旦偶然使用了这个命令游戏将会直接退出，你将会被询问你是否需要退出。
默认的回应是\f(CRn\fP（no）；游戏继续。想要真正退出，回答\f(CRy\fP。
你可以设置
.op paranoid_confirmation:quit
选项来要求回答\f(CRyes\fP。
.lp "#quiver  "
选择准备弹药。
默认按键为‘Q’。
.lp "#read    "
读卷轴或者书。
默认按键为‘r’。
.lp "#redraw  "
刷新屏幕。
默认按键为‘^R’，如果
.op number_pad
选项启用也可以使用‘^L’。
.lp "#remove  "
摘下配饰（戒指，护身符等）。
默认按键为‘R’。
.lp "#ride    "
上坐骑或下坐骑。
自动完成。
默认按键为‘M-R’。
.lp "#rub     "
摩擦一盏灯或一块石头。
自动完成。
默认按键为‘M-r’。
.lp "#save    "
保存并退出。
默认按键为‘S’。
.lp "#search  "
搜索陷阱和暗门。
默认按键为‘s’。
.lp "#seeall  "
显示所有正在使用的装备。
默认按键为‘*’。
.lp #seeamulet
显示正在戴着的护身符。
默认按键为‘"’。
.lp #seearmor
显示正在穿着的防具。
默认按键为‘[’。
.lp "#seegold "
计算你当前的金币。
默认按键为‘$’。
.lp "#seenv   "
显示地图位置的‘已查看’向量。
自动完成。Debug mode only.
.lp #seerings
显示正在戴着的戒指。
默认按键为‘=’。
.lp #seespells
列举并重新排序已知法术。
默认按键为‘+’。
.lp #seetools
显示正在使用的工具。
默认按键为‘（’。
.lp "#seetrap "
显示附近一个陷阱的类型。
默认按键为‘^’。
.lp #seeweapon
显示正在装备的武器。
默认按键为‘）’。
.lp "#shell   "
退出游戏以进入子终端（输入'exit'即可返回）。
默认按键为‘!’。
.lp "#sit     "
坐下。
自动完成。
默认按键为‘M-s’。
.lp "#stats   "
显示记忆统计数据。
自动完成。调试模式专属。
.lp "#suspend "
将游戏推至后台（输入'fg'返回）。
默认按键为‘^Z’。
.lp "#swap    "
切换主武器和副武器。
默认按键为‘x’。
.lp "#takeoff "
脱下一件防具。
默认按键为‘T’。
.lp #takeoffall
脱下所有防具。
默认按键为‘A’。
.lp #teleport
层间传送。
默认按键为‘^T’。
.lp "#terrain "
显示没有怪物，对象，或者陷阱的纯地图。
自动完成。
.lp #therecmdmenu
在此处对相邻位置可执行的指令菜单。
.lp "#throw   "
扔东西。
默认按键为‘t’。
.lp "#timeout "
查看倒计时和英雄的定时内置函数。调试模式专属。
.lp "#tip     "
倒空容器。
自动完成。
默认按键为‘M-T’。使用‘m’前缀让这个命令使用一个菜单。
.lp "#travel  "
自动旅行到地图上的特定位置。
默认按键为‘_’。使用“request menu”前缀来显示一个在视野中感兴趣的不要求移动光标到指定位置的目标的菜单。
当用光标选择一个目标并且
.op autodescribe
选项启用的时候，屏幕顶部文本行将会显示“（no travel path，没有行进路线）”如果你的角色不知道如何到达那个位置。
.lp "#turn    "
驱赶亡灵。
自动完成。
默认按键为‘M-t’。
.lp #twoweapon
开关双持战斗。
自动完成。
默认按键为‘X’。如果
.op number_pad
选项关闭也可以使用‘M-2’。
.lp ""
需要注意的是你必须要使用合适的武器来进行这种类型的战斗，否则双持武器模式将会自动关闭。
.lp "#untrap  "
解除陷阱。
默认按键为‘M-u’，如果
.op number_pad
选项启用也可以使用‘u’。
.lp ""
在某些情形下（circumstance）该命令也可以用于解救被陷阱困住的怪物。
.lp "#up      "
上楼梯。
默认按键为‘<’。
.lp #vanquished
列举击败的怪物。
自动完成。调试模式专属。
.lp "#version "
显示此版本NetHack的编译时选项。
自动完成。
默认按键为‘M-v’。
.lp #versionshort
显示程序版本和编译时间。
默认按键为‘v’。
.lp "#vision  "
显示视觉阵列。
自动完成。调试模式专属。
.lp "#wait    "
停下，什么都不做。
默认按键为‘.’，如果
.op rest_on_space
选项开启也可以使用‘ ’（空格）。
.lp "#wear    "
穿上一件防具。
默认按键为‘W’。
.lp #whatdoes
说明指令的作用。
默认按键为‘&’。
.lp "#whatis  "
说明符号对应的事物。
默认按键为‘/’。
.lp "#wield   "
装备（使用）武器。
默认按键为‘w’。
.lp "#wipe    "
擦脸。
自动完成。
默认按键为‘M-w’。
.lp "#wizbury "
将物品埋在你身下和周围。
自动完成。调试模式专属。
.lp #wizdetect
揭示小范围内隐藏的事物。
自动完成。调试模式专属。
默认按键为‘^E’。
.lp #wizgenesis
创造怪物。可以通过加上一个数字前缀来创建对应数量的怪物。
自动完成。调试模式专属。
默认按键为‘^G’。
.lp #wizidentify
识别物品栏中的所有物品。
自动完成。调试模式专属。
默认按键为‘^I’。
.lp #wizintrinsic
设定内在属性。
自动完成。调试模式专属。
.lp #wizlevelport
传送到另一层。
自动完成。调试模式专属。
默认按键为‘^V’。
.lp "#wizmap  "
为该层画出地图。
自动完成。调试模式专属。
默认按键为‘^F’。
.lp #wizrumorcheck
验证传言的真实性。
自动完成。调试模式专属。
.lp #wizsmell
嗅探怪物。
自动完成。调试模式专属。
.lp #wizwhere
显示特殊关卡的位置。
自动完成。调试模式专属。
.lp "#wizwish "
许愿。
自动完成。调试模式专属。
默认按键为‘^W’。
.lp "#wmode   "
显示墙模式。
自动完成。调试模式专属。
.lp "#zap     "
挥舞魔杖。
默认按键为‘z’。
.lp "#?       "
列举所有扩展指令。